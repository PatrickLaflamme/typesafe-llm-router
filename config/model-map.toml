# Maps router catalog tiers → Cursor model ids (placeholders until Cursor.models.list).
# Loaded by CursorAgentSdkSource::list_models.

[tiers.T-small]
cursor_model_id = "composer-2.5"
label = "placeholder small→composer (replace after models.list)"

[tiers.T-mid]
cursor_model_id = "composer-2.5"
label = "placeholder mid→composer"

[tiers.T-frontier]
cursor_model_id = "gpt-5"
label = "placeholder frontier — replace after Cursor.models.list"
